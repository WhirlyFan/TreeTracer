import { Card, Typography } from "antd";

const { Text } = Typography;

export default function Settings() {
  return (
    <Card style={{ textAlign: "center", marginTop: "20px" }}>
      <Text strong>Feature Coming Soon...</Text>
    </Card>
  );
}
